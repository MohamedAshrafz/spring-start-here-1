package start.here;

public enum NAMES {

    EMAIL_NOTIFICATION_NAME("emailNotificationProxy"),
    PUSH_NOTIFICATION_PROXY("pushNotificationProxy");

    public final String title;

    NAMES(String title) {
        this.title = title;
    }
}
